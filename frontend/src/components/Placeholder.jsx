const PlaceHolder = ({ title }) => (
    <div style={{ padding: '2rem' }}>
        <h1 style={{ color: 'var(--color-social)' }}>{title}</h1>
        <p>Coming soon...</p>
    </div>
);
export default PlaceHolder;
